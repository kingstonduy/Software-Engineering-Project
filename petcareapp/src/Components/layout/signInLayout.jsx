import React from 'react';
import Login from '../Body/Login/signIn/Login';

export const SignInLayout = () => {
    return (
        <div style={{ height: '100%', overflow: 'hidden' }}>
            <Login />
        </div>
    );
};
